---
const education = [
  {
    degree: 'Specialization in Aeronautical Systems',
    institution: 'Escola de Engenharia de São Carlos (EESC), USP',
    year: '2025',
    focus: 'Constraint-Aware Fault-Tolerant Multi-Agent UAV System Using OODA Loop: Realistic Mission Completion Assistance',
    description: 'Framework for on-the-fly UAV avionics generation — AI extracts operational knowledge via RAG and knowledge graphs.',
    publication: true,
  },
  {
    degree: 'Master of Science in Biotechnology',
    institution: 'Universidade Federal de São Carlos',
    year: '2024',
    focus: 'AI-driven biomedical literature mining using RAG and knowledge graphs',
    description: 'Use of Artificial Intelligence in the search for relationships between COVID-19 and Vitamin D.',
  },
  {
    degree: 'Bachelor of Science in Computational Physics',
    institution: 'University of São Paulo (USP)',
    year: '2015',
    focus: 'Magnetoelectric Multiferroic Composites',
    description: 'Interdisciplinary program: Physics, Mathematics, Computer Science. Research on synthesis and characterization of PbO-free composites (GCFerr Lab).',
  },
];

const researchAreas = [
  'Autonomous Decision-Making Systems',
  'Knowledge Management',
  'Computer Vision & OCR',
  'Ontologies',
  'Embedded Real-time Systems',
  'UAV Systems',
];
---

<section class="research" id="research">
  <div class="container">
    <div class="research-header fade-in">
      <p class="section-label">Research & Education</p>
      <h2 class="section-title">Academic work</h2>
      <p class="section-subtitle">
        Three degrees, two published papers, and ongoing work in aeronautical systems.
      </p>
    </div>

    <div class="research-layout">
      <div class="education-list">
        {education.map((edu, i) => (
          <div class="education-card fade-in" style={`transition-delay: ${i * 0.1}s`}>
            <time class="edu-year">{edu.year}</time>
            <div class="edu-content">
              <h3 class="edu-degree">{edu.degree}</h3>
              <p class="edu-institution">{edu.institution}</p>
              <p class="edu-focus"><strong>Research:</strong> {edu.focus}</p>
              <p class="edu-description">{edu.description}</p>
              {edu.publication && (
                <span class="edu-publication">Published research</span>
              )}
            </div>
          </div>
        ))}
      </div>

      <div class="research-areas fade-in">
        <h3 class="areas-title">Research interests</h3>
        <div class="areas-tags">
          {researchAreas.map((area) => (
            <span class="area-tag">{area}</span>
          ))}
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .research {
    padding-inline: 2rem;
    padding-bottom: 4rem;
    background: var(--color-bg-warm);
  }

  .research-header {
    margin-bottom: 3rem;
  }

  .research-layout {
    display: grid;
    grid-template-columns: 2fr 1fr;
    gap: 3rem;
    align-items: start;
  }

  .education-list {
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .education-card {
    display: grid;
    grid-template-columns: 120px 1fr;
    gap: 1.25rem;
    padding: 1.75rem 0;
    border-bottom: 1px solid var(--color-border);
  }

  .education-card:first-child {
    padding-top: 0;
  }

  .education-card:last-child {
    border-bottom: none;
    padding-bottom: 0;
  }

  .edu-year {
    font-size: 0.8rem;
    font-weight: 600;
    color: var(--color-text-muted);
    padding-top: 0.15rem;
  }

  .edu-degree {
    font-size: 1rem;
    font-weight: 700;
    color: var(--color-text-dark);
    margin-bottom: 0.25rem;
  }

  .edu-institution {
    font-size: 0.825rem;
    color: var(--color-text-muted);
    margin-bottom: 0.6rem;
  }

  .edu-focus {
    font-size: 0.8rem;
    color: var(--color-text-dark);
    margin-bottom: 0.35rem;
  }

  .edu-description {
    font-size: 0.8rem;
    color: var(--color-text-muted);
    line-height: 1.6;
  }

  .edu-publication {
    display: inline-block;
    margin-top: 0.5rem;
    font-size: 0.7rem;
    font-weight: 600;
    color: var(--color-accent-dim);
    background: var(--color-accent-glow);
    padding: 0.2rem 0.6rem;
    border-radius: 4px;
  }

  .research-areas {
    background: var(--color-bg-card);
    border: 1px solid var(--color-border);
    border-radius: 12px;
    padding: 1.75rem;
    position: sticky;
    top: calc(var(--header-height) + 1.5rem);
  }

  .areas-title {
    font-size: 0.9rem;
    font-weight: 700;
    color: var(--color-text-dark);
    margin-bottom: 1rem;
  }

  .areas-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.6rem;
  }

  .area-tag {
    font-size: 0.8rem;
    font-weight: 500;
    color: var(--color-text-muted);
    background: var(--color-bg-light);
    border: 1px solid var(--color-border);
    padding: 0.35rem 0.8rem;
    border-radius: 20px;
    transition: border-color 0.2s, color 0.2s, background 0.2s;
  }

  .area-tag:hover {
    border-color: var(--color-accent);
    color: var(--color-accent);
    background: var(--color-accent-glow);
  }

  @media (max-width: 1024px) {
    .research-layout {
      grid-template-columns: 1fr;
    }

    .research-areas {
      position: static;
    }
  }

  @media (max-width: 768px) {
    .research {
      padding-inline: 1.25rem;
    }

    .education-card {
      grid-template-columns: 1fr;
      gap: 0.25rem;
    }

    .research-areas {
      position: static;
    }

    .edu-publication {
      font-size: 0.75rem;
    }
  }
</style>
